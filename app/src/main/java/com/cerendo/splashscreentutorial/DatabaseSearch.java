package com.cerendo.splashscreentutorial;

import androidx.appcompat.app.AppCompatActivity;

import android.os.Bundle;
import android.view.View;
import android.widget.ArrayAdapter;
import android.widget.EditText;

import java.util.List;

public class DatabaseSearch extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_database_search);
    }

    public void onClickSearchByName(View view) {
        // function that needs to be written in relation to database
    }

    public void onClickSearchByAddress(View view) {
        // function that needs to be written in relation to database

    }
}